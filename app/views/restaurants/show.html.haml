= render 'shared/header', title: @title, image: image_path(@model), additional_header_content: 'restaurants/user_options', warning: true
.ui.grid.centered.container.page-content
  = render 'items/items', items: @items, item_type_scopes: @item_type_scopes
  .row
    .ui.container
      = render 'restaurants/options'
