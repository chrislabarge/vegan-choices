  <div class="list-group">
<% ingredients.each do |ingredient|%>
    <div class='list-group-item'>
      <%= (render 'ingredient', ingredient: ingredient) if ingredient %>
    </div>
<% end %>
  </div>
