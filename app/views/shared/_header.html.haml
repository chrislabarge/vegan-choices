- image ||= nil
.ui.center.aligned.page-header
  %h1.ui.header
    = title
  - if image
    = image_tag(image, height: '80', width:'80', class: 'ui image middle aligned')
  - if defined?(subtitle)
    %h2.sub.header
      = subtitle
