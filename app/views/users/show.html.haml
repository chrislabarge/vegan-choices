= render 'shared/header', title: @title, additional_header_content: 'users/user_options', editable: !@visitor.present?, image: 'users/avatar.png'
.ui.grid.centered.container.page-content.dashboard
  - if (@list_items)
    .sixteen.wide.mobile.twelve.wide.computer.column
      = render 'shared/list', header_options: {list_title: @list_title, row_title: 'Recent Activity', multiple_lists: render('dashboard_lists').html_safe}, items: @list_items, row_view: @list_row
