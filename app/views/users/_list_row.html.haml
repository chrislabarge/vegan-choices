%a.item{:href => "#{user_path(model)}"}
  .content
    .ui.grid.middle.aligned
      .eight.wide.mobile.column
        %h3.ui.header.user-title
          = image_tag(avatar_path(model, :thumb), class: 'ui image middle aligned')
          = model.name
      .eight.wide.mobile.column.right.aligned.item-count
        -# %h4
        -#   %span.highlight
        -#     = model.items.count
        -#   %strong
        -#     Options
